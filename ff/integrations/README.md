# Inspirations
Some Inspirational github repos.

```
git clone https://github.com/microsoft/vscode.git && cd vscode && rm -rf .git && cd ..
git clone https://github.com/sadmann7/shadcn-table.git table && cd table && rm -rf .git && cd ..
git clone https://github.com/gantoreno/vscode-vercel.git vscode-vercel && cd vscode-vercel && rm -rf .git && cd ..
git clone https://github.com/hoovercj/vscode-power-mode power-mode && cd power-mode && rm -rf .git && cd ..
git clone https://github.com/PavelDoGreat/WebGL-Fluid-Simulation fluid && cd fluid && rm -rf .git && cd ..
git clone https://github.com/Qix-/color-convert colors && cd colors && rm -rf .git && cd ..
git clone https://github.com/Evercoder/culori color && cd color && rm -rf .git && cd ..

git clone https://github.com/zaidmukaddam/scira-mcp-chat && cd scira-mcp-chat && rm -rf .git
git clone https://github.com/cgoinglove/mcp-client-chatbot mcp && cd mcp && rm -rf .git
git clone https://github.com/n8n-io/n8n.git && cd n8n && rm -rf .git
git clone https://github.com/excalidraw/excalidraw && cd excalidraw && rm -rf .git
git clone https://github.com/udecode/plate && cd plate && rm -rf .git
```
